package kotlin

// interfaces to spoof acutal kotlin interfaces to match in service

interface Function0<out R>
interface Function1<in T, out R>
interface Function2<in Z, in I, out O>
interface Function3<in Z, in I, in O, out B>
class Any
